# 인수인계 — 모바일 청첩장 (wed)

무엇을 어떻게 고치는지는 [README.md](README.md). 이 문서는 **진행상황과 함정**만 다룬다.

- 배포 = GitHub Pages, `main` 에 push 하면 1~2분 뒤 자동 반영
- 주소 = https://dylan-bak.github.io/wedding-invitation/
- 로컬 확인 = `npx serve -l 4321` 뒤 `http://localhost:4321`

## 1. 남은 작업

| # | 내용 | 누가 |
|---|---|---|
| P1 | 실기기 확인 — iOS Safari · Android Chrome. 헤드리스 Chrome 으로만 검증한 상태 | 사용자 |
| P2 | 배경음악 소리 확인. 클로드는 소리를 못 들으므로 곡이 어울리는지 판단 불가 | 사용자 |

P1 에서 볼 것 = ① 계단 아래에 크림색 빈 띠가 생기는지(주소창이 내려간 상태) ② 문 열림이 끊기지 않는지 ③ 배경음악 안내가 뜨고 3초 뒤 사라지는지.

## 2. 사용자 영역 (클로드가 못 하는 것)

| 대상 | 왜 | 대신 할 수 있는 것 |
|---|---|---|
| Gemini 이미지 생성 | `aiplatform.endpoints.predict` 권한 없음 → HTTP 403. `GEMINI_API_KEY` 도 미설정 | 프롬프트를 만들어 주면 사용자가 Gemini 앱에서 생성 |
| 음악 생성(Lyria) | 리전 엔드포인트 `us-central1-aiplatform.googleapis.com` 이 네트워크에서 차단(연결 타임아웃). global 엔드포인트는 403 | Pixabay 등에서 내려받기 (승인 후) |
| Apps Script 배포 | 사내망에서 Google 로그인 차단 | 집 PC 에서 사용자가 수행 |

## 3. 함정 — 다시 밟지 말 것

- **대문 사진 자산을 바꾸면 문 좌표를 픽셀 밝기로 다시 재야 한다.** 눈대중으로 잡으면 문짝 옆에 밝은 문틀이 몇 px 드러나 흰 세로줄이 생긴다. 실측 방법은 README §4.
- **`svh` 로 첫 화면 높이를 잡으면 안 된다.** 모바일에서 주소창이 내려가는 순간 계단 아래에 배경색 띠가 드러난다. `lvh` 기준으로 두고, 스크롤 안내만 `calc(100lvh - 96svh)` 로 올려 작은 화면에서도 보이게 했다.
- **참석 회신 시트 이름(`tools/rsvp.gs` 의 `SHEET_NAME`)은 실제 배포본과 일치해야 한다.** 어긋나면 회신이 두 시트로 갈린다. 현재 `모청 참석 회신 v3`.

## 4. 종결 — 다시 검토하지 말 것

| 대상 | 판정 | 사유 |
|---|---|---|
| Gemini 로 더 큰 대문 사진 받기 | 불필요 | 1400×2352 를 요청해도 800×1344 로만 출력(2회 실측). 지금은 촬영 원본 door-2(1400px)를 쓰므로 더 나음 |
| 문 회전각을 화면 안에 담기 | 불가 | 문짝 폭이 화면 폭의 87% 라 83°부터 자유단이 화면 밖. 원근을 없애도 동일 |
| 종이 청첩장 QR 재발급 | 불필요 | 주소가 그대로면 기존 QR 이 계속 유효 |
| AWS 호스팅 | 대안 | GitHub Pages 로 확정. 절차는 README §9 에 보존 |

## 5. 되돌릴 기준점

`git tag door-ok-inward` (커밋 `a39a988`) = 문 열림 연출이 처음 정상 동작한 시점.
