# 인수인계 — 모바일 청첩장 (wed)

무엇을 어떻게 고치는지는 [README.md](README.md). 이 문서는 **남은 작업**과 **손대면 깨지는 곳**만 다룬다.

- 배포 = GitHub Pages, `main` 에 push 하면 1~2분 뒤 자동 반영
- 주소 = https://dylan-bak.github.io/wedding-invitation/
- 로컬 확인 = `npx serve -l 4321` 뒤 `http://localhost:4321`

## 1. 남은 작업

| # | 내용 | 누가 |
|---|---|---|
| P1 | 실기기 확인 — iOS Safari · Android Chrome. 본문(참고안 구성 전체)은 헤드리스 Chrome 으로만 검증 | 사용자 |
| P3 | iOS 문 내려올 때 떨림 — 기본판(줌 없음 + 대책 4건, README §4)에서 사라졌는지. 남으면 dev 모드(줌 켬)와 비교해 줌 탓인지 가른다 | 사용자 판정 → 클로드가 `maxZoom` 고정 + 7번 탭·dev 표식 코드 삭제 |
| P4 | 지도를 캡처 이미지에서 실제 지도로 바꿀지 — 바꾼다면 카카오 JavaScript 키 발급(README §4 오시는 길). 안 바꾸면 지금 이미지 유지 | 사용자 결정 · 키 발급 |

P1 에서 볼 것 = ① 계단 아래에 크림색 빈 띠가 생기는지(주소창이 내려간 상태) ② 문 열린 뒤 표지(OUR WEDDING + 아치 사진 + 일시)가 한 화면에 다 들어오는지 ③ 남은 시간이 1초마다 바뀌는지 ④ 계좌 복사 버튼이 실제로 복사하는지(인앱 브라우저 포함) ⑤ 공유하기가 기기 공유창을 띄우는지 ⑥ 티맵 버튼이 앱을 열거나 스토어로 가는지.

## 2. 손대면 깨지는 곳 — 고치기 전에 읽을 것

- **대문 사진 자산을 바꾸면 문 좌표를 픽셀 밝기로 다시 재야 한다.** 눈대중으로 잡으면 문짝 옆에 밝은 문틀이 몇 px 드러나 흰 세로줄이 생긴다. 실측 방법은 README §4.
- **`svh` 로 첫 화면 높이를 잡으면 안 된다.** 모바일에서 주소창이 내려가는 순간 계단 아래에 배경색 띠가 드러난다. `lvh` 기준으로 두고, 스크롤 안내만 `calc(100lvh - 96svh)` 로 올려 작은 화면에서도 보이게 했다.
- **참석 회신 시트 이름(`tools/rsvp.gs` 의 `SHEET_NAME`)은 실제 배포본과 일치해야 한다.** 어긋나면 회신이 두 시트로 갈린다. 현재 `모청 참석 회신 v3`.

## 3. 종결 — 다시 검토하지 말 것

| 대상 | 판정 | 사유 |
|---|---|---|
| Gemini 로 더 큰 대문 사진 받기 | 불필요 | 1400×2352 를 요청해도 800×1344 로만 출력(2회 실측). 지금은 촬영 원본 door-2(1400px) 합성이라 더 선명 |
| 배경음악 AI 생성 | 불필요 | Pixabay 무료 곡 채택으로 확정 |
| 문 회전각을 화면 안에 담기 | 불가 | 문짝 폭이 화면 폭의 87% 라 83°부터 자유단이 화면 밖. 원근을 지워도 동일 |
| 종이 청첩장 QR 재발급 | 불필요 | 주소가 그대로면 기존 QR 이 계속 유효 |
| AWS 호스팅 | 대안 | GitHub Pages 로 확정. 절차는 README §9 에 보존 |

## 4. 되돌릴 기준점

`git tag door-ok-inward` (커밋 `a39a988`) = 문 열림 연출이 처음 정상 동작한 시점.
